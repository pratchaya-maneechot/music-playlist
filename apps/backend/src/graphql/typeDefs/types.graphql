type User {
  id: ID!
  username: String!
  email: String!
  createdAt: String!
  updatedAt: String!
}

type Playlist {
  id: ID!
  name: String!
  userId: ID!
  songs: [Song!]
  createdAt: String!
  updatedAt: String!
}

type Song {
  id: ID!
  title: String!
  artist: String!
  album: String!
  duration: String!
  createdAt: String!
  updatedAt: String!
}
